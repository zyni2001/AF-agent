# FOLIO Agent 系统完整流程说明

## 📋 目录
1. [系统架构概览](#系统架构概览)
2. [核心组件说明](#核心组件说明)
3. [本地测试流程](#本地测试流程)
4. [云端部署流程](#云端部署流程)
5. [AgentBeats平台集成](#agentbeats平台集成)
6. [评估执行流程](#评估执行流程)
7. [数据流转过程](#数据流转过程)

---

## 系统架构概览

这是一个基于 **AgentBeats** 平台的逻辑推理评估系统，用于评估AI代理在一阶逻辑推理任务上的表现。

```
┌─────────────────────────────────────────────────────────────┐
│                    FOLIO 逻辑推理评估系统                      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────┐         ┌──────────────────┐
│   Green Agent   │────────▶│  White Agents    │
│  (评估器/考官)   │   A2A   │  (被评估者)      │
│                 │ Protocol│                  │
│ - 加载数据集     │         │ - Baseline Agent │
│ - 发送问题       │         │ - Autoform Agent │
│ - 比较答案       │         │                  │
│ - 生成报告       │         │                  │
└─────────────────┘         └──────────────────┘
```

### 三种运行模式

1. **本地模式**: 所有组件在本地运行，用于开发和测试
2. **混合模式**: 本地Green Agent评估云端部署的White Agents
3. **云端模式**: 所有组件部署到Google Cloud Run，通过AgentBeats平台管理

---

## 核心组件说明

### 1. Green Agent (评估器/考官)

**职责**: 评估White Agents的表现

**工作流程**:
```
1. 加载FOLIO验证数据集 (204个逻辑推理问题)
   ↓
2. 解析评估任务 (从用户或AgentBeats平台接收)
   ↓
3. 遍历数据集中的每个问题
   ↓
4. 通过A2A协议发送问题给White Agent
   ↓
5. 接收White Agent的回答 (True/False/Uncertain)
   ↓
6. 与标准答案比较
   ↓
7. 计算准确率、统计信息
   ↓
8. 生成评估报告
```

**关键代码位置**: `src/green_agent/agent.py`

### 2. Baseline White Agent (基线代理)

**职责**: 使用直接LLM推理回答问题

**工作流程**:
```
1. 接收问题 (自然语言的前提和结论)
   ↓
2. 构建提示词，要求LLM进行逻辑推理
   ↓
3. 调用Gemini 2.0 Flash模型
   ↓
4. 解析LLM响应
   ↓
5. 返回: True / False / Uncertain
```

**特点**:
- 使用 `gemini-2.0-flash-lite` 模型
- 直接依赖LLM的推理能力
- 不需要形式化逻辑

**关键代码位置**: `src/white_agent_baseline/agent.py:70-170`

### 3. Autoformalization White Agent (自动形式化代理)

**职责**: 将自然语言问题转换为可执行的Z3代码

**工作流程**:
```
1. 接收问题 (自然语言的前提和结论)
   ↓
2. 使用LLM生成Z3 Python代码
   ↓
3. 执行生成的代码
   ↓
4. 如果执行失败，尝试修复 (最多3次)
   ↓
5. 解析Z3求解器的结果
   ↓
6. 返回: True / False / Uncertain
```

**特点**:
- 使用形式化逻辑 (Z3 SMT求解器)
- 可以处理复杂的逻辑推理
- 有自动修复机制

**关键代码位置**: `src/white_agent_autoform/agent.py`

---

## 本地测试流程

### 步骤1: 环境准备

```bash
# 1. 安装依赖
uv sync  # 或: pip install -e .

# 2. 设置API密钥
export GEMINI_API_KEY='your_api_key_here'
```

### 步骤2: 运行评估

```bash
# 快速测试 (5个例子，只测试autoform agent)
python main.py quick

# 测试10个例子，两个agent都测试
python main.py launch --max 10 --both

# 完整评估 (所有204个例子)
python main.py full
```

### 步骤3: 执行过程详解

当运行 `python main.py launch --max 10 --both` 时：

```
┌─────────────────────────────────────────────────────────┐
│  Step 1: 启动Green Agent (端口9001)                    │
│  - 加载FOLIO数据集                                      │
│  - 启动HTTP服务器                                       │
│  - 等待就绪信号                                         │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│  Step 2a: 启动Baseline Agent (端口9002)                 │
│  - 初始化LLM客户端                                      │
│  - 启动HTTP服务器                                       │
│  - 等待就绪信号                                         │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│  Step 2b: 启动Autoform Agent (端口9003)                 │
│  - 初始化LLM和Z3环境                                    │
│  - 启动HTTP服务器                                       │
│  - 等待就绪信号                                         │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│  Step 3: 评估Baseline Agent                             │
│  - Green Agent发送10个问题给Baseline Agent              │
│  - 收集回答并比较                                       │
│  - 生成报告                                             │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│  Step 4: 评估Autoform Agent                              │
│  - Green Agent发送10个问题给Autoform Agent               │
│  - 收集回答并比较                                       │
│  - 生成报告                                             │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│  Step 5: 清理                                           │
│  - 终止所有进程                                         │
│  - 显示总结                                             │
└─────────────────────────────────────────────────────────┘
```

### 步骤4: 查看结果

评估完成后会显示：
- 准确率 (Accuracy)
- 正确/错误数量
- 每个问题的详细结果
- 平均处理时间

---

## 云端部署流程

### 部署架构

```
┌─────────────────────────────────────────────────────────┐
│              Google Cloud Run 部署                        │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  ┌──────────────────┐    ┌──────────────────┐          │
│  │ Baseline Agent   │    │ Autoform Agent    │          │
│  │ Cloud Run        │    │ Cloud Run        │          │
│  │                  │    │                  │          │
│  │ - Docker容器     │    │ - Docker容器     │          │
│  │ - AgentBeats     │    │ - AgentBeats     │          │
│  │   Controller     │    │   Controller     │          │
│  │ - 端口: 8080     │    │ - 端口: 8080     │          │
│  └──────────────────┘    └──────────────────┘          │
│                                                           │
│  ┌──────────────────┐                                    │
│  │ Green Agent      │                                    │
│  │ Cloud Run        │                                    │
│  │                  │                                    │
│  │ - Docker容器     │                                    │
│  │ - AgentBeats     │                                    │
│  │   Controller     │                                    │
│  │ - 端口: 8080     │                                    │
│  └──────────────────┘                                    │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

### 部署步骤

#### 1. 部署Baseline Agent

```bash
./deploy_baseline.sh
```

**执行过程**:
```
1. 检查Google Cloud SDK
   ↓
2. 配置项目ID和区域
   ↓
3. 输入Gemini API密钥
   ↓
4. 启用必要的API (Cloud Run, Cloud Build, Container Registry)
   ↓
5. 使用Cloud Build构建Docker镜像
   ↓
6. 部署到Cloud Run
   ↓
7. 设置环境变量:
   - GEMINI_API_KEY
   - PUBLIC_URL (服务URL)
   - CLOUDRUN_HOST
   - AGENT_ROLE=baseline
   ↓
8. 返回服务URL
```

**生成的URL示例**: 
```
https://folio-baseline-agent-xxxxx-uc.a.run.app
```

#### 2. 部署Autoform Agent

```bash
./deploy_autoform.sh
```

**过程类似，但设置**:
- `AGENT_ROLE=autoform`

#### 3. 部署Green Agent

```bash
./deploy_green.sh
```

**过程类似，但设置**:
- `AGENT_ROLE=green`
- 内存: 2Gi (需要更多内存处理数据集)

### Docker容器结构

每个Docker容器包含:

```
┌─────────────────────────────────────┐
│         Docker Container            │
├─────────────────────────────────────┤
│                                       │
│  ┌─────────────────────────────┐   │
│  │  AgentBeats Controller      │   │
│  │  (earthshaker)              │   │
│  │  - 管理agent生命周期         │   │
│  │  - 提供代理路径              │   │
│  │  - 端口: 8080               │   │
│  └─────────────────────────────┘   │
│              ↓                       │
│  ┌─────────────────────────────┐   │
│  │  Agent Application          │   │
│  │  (A2A Starlette App)        │   │
│  │  - 处理A2A协议请求          │   │
│  │  - 执行agent逻辑             │   │
│  │  - 端口: AGENT_PORT          │   │
│  └─────────────────────────────┘   │
│                                       │
└─────────────────────────────────────┘
```

**关键点**:
- Controller监听8080端口 (Cloud Run要求)
- Agent运行在内部端口 (由AGENT_PORT环境变量指定)
- Controller通过代理路径 `/to_agent/{agent_id}/...` 转发请求

---

## AgentBeats平台集成

### AgentBeats是什么？

AgentBeats是一个**AI代理评估平台**，提供:
- 标准化的代理通信协议 (A2A)
- 代理管理和监控
- 评估任务调度
- 结果可视化

### 注册流程

#### 1. 在AgentBeats平台注册代理

访问: https://v2.agentbeats.org

**注册Baseline Agent**:
```
名称: FOLIO-white-CoT
类型: Assessee (White Agent)
部署类型: Remote
Controller URL: https://folio-baseline-agent-xxxxx-uc.a.run.app
```

**注册Autoform Agent**:
```
名称: FOLIO-autoform-agent
类型: Assessee (White Agent)
部署类型: Remote
Controller URL: https://folio-autoform-agent-xxxxx-uc.a.run.app
```

**注册Green Agent**:
```
名称: FOLIO-green-agent
类型: Assessor (Green Agent)
部署类型: Remote
Controller URL: https://folio-green-agent-xxxxx-uc.a.run.app
```

#### 2. 验证代理状态

平台会自动检查:
- ✅ Controller是否可达
- ✅ Agent Card是否有效
- ✅ Agent是否正在运行

#### 3. 创建评估任务

```
1. 进入 "My Assessments"
2. 点击 "+" 创建新评估
3. 选择:
   - Green Agent: FOLIO-green-agent
   - White Agent: FOLIO-autoform-agent (或 FOLIO-white-CoT)
4. 点击 "Create Assessment"
```

#### 4. 运行评估

```
1. 在评估列表中找到创建的评估
2. 点击 "Start Assessment"
3. 平台会:
   - 实例化Green Agent和White Agent
   - 发送评估任务给Green Agent
   - 实时显示日志
   - 显示最终结果
```

---

## 评估执行流程

### 完整的数据流

```
┌─────────────────────────────────────────────────────────────┐
│  用户/AgentBeats平台                                         │
│  发送评估任务:                                               │
│  <white_agent_url>                                          │
│  https://folio-baseline-agent-xxxxx.run.app                 │
│  </white_agent_url>                                         │
│  <max_examples>10</max_examples>                            │
└─────────────────────────────────────────────────────────────┘
                    ↓ (A2A Protocol)
┌─────────────────────────────────────────────────────────────┐
│  Green Agent (评估器)                                        │
│  1. 解析任务，提取white_agent_url和max_examples             │
│  2. 加载FOLIO数据集                                          │
│  3. 限制到前10个例子                                         │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│  遍历每个问题 (循环)                                          │
│                                                              │
│  问题示例:                                                   │
│  Premises:                                                  │
│  - Alice performs often in talent shows                     │
│  - If someone performs often, they attend school events    │
│    and are engaged                                          │
│  Conclusion:                                                │
│  - Alice attends school events and is engaged              │
│                                                              │
│  Expected Answer: True                                      │
└─────────────────────────────────────────────────────────────┘
                    ↓ (A2A Protocol - send_message)
┌─────────────────────────────────────────────────────────────┐
│  White Agent (被评估者)                                      │
│                                                              │
│  ┌──────────────────────────────────────┐                 │
│  │ Baseline Agent路径:                  │                 │
│  │ 1. 构建提示词                        │                 │
│  │ 2. 调用Gemini模型                    │                 │
│  │ 3. 解析响应 → "True"                │                 │
│  └──────────────────────────────────────┘                 │
│                                                              │
│  ┌──────────────────────────────────────┐                 │
│  │ Autoform Agent路径:                  │                 │
│  │ 1. LLM生成Z3代码                     │                 │
│  │ 2. 执行Z3代码                        │                 │
│  │ 3. 解析结果 → "True"                 │                 │
│  └──────────────────────────────────────┘                 │
└─────────────────────────────────────────────────────────────┘
                    ↓ (A2A Protocol - response)
┌─────────────────────────────────────────────────────────────┐
│  Green Agent (评估器)                                        │
│  1. 接收回答: "True"                                        │
│  2. 与标准答案比较: True == True ✓                          │
│  3. 记录结果                                                │
└─────────────────────────────────────────────────────────────┘
                    ↓ (继续下一个问题)
┌─────────────────────────────────────────────────────────────┐
│  所有问题处理完成                                             │
│  1. 计算统计信息:                                            │
│     - 总问题数: 10                                          │
│     - 正确数: 8                                             │
│     - 错误数: 2                                             │
│     - 准确率: 80.00%                                        │
│  2. 生成报告                                                 │
│  3. 返回给用户/平台                                          │
└─────────────────────────────────────────────────────────────┘
```

### A2A协议通信细节

**A2A (Agent-to-Agent) 协议**是Google开发的标准化代理通信协议。

**消息格式** (JSON-RPC):
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "send_message",
  "params": {
    "message": {
      "role": "user",
      "parts": [
        {
          "text": "Premises: ...\nConclusion: ...\n\nAnswer:"
        }
      ]
    }
  }
}
```

**响应格式**:
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "message": {
      "role": "agent",
      "parts": [
        {
          "text": "True"
        }
      ]
    }
  }
}
```

---

## 数据流转过程

### 1. 问题数据格式

**FOLIO数据集中的每个问题**:
```python
{
  "story_id": 380,
  "premises": [
    "Alice performs often in talent shows",
    "If someone performs often, they attend school events and are engaged"
  ],
  "conclusion": "Alice attends school events and is engaged",
  "label": "True"  # 标准答案
}
```

### 2. 发送给White Agent的格式

**Green Agent构建的提示词**:
```
Premises:
- Alice performs often in talent shows
- If someone performs often, they attend school events and are engaged

Conclusion:
Alice attends school events and is engaged

Answer:
```

### 3. Baseline Agent处理流程

```python
# 1. 接收用户输入
user_input = "Premises: ...\nConclusion: ...\n\nAnswer:"

# 2. 构建消息历史
messages = [
    {
        "role": "system",
        "content": "You are a logical reasoning expert. Your response must be EXACTLY one word: True, False, or Uncertain"
    },
    {
        "role": "user",
        "content": user_input
    }
]

# 3. 调用LLM
response = completion(
    messages=messages,
    model="gemini/gemini-2.0-flash-lite",
    temperature=0.0
)

# 4. 提取回答
answer = response.choices[0].message.content  # "True"
```

### 4. Autoform Agent处理流程

```python
# 1. 接收用户输入
user_input = "Premises: ...\nConclusion: ...\n\nAnswer:"

# 2. 生成Z3代码
prompt = f"""Convert this logical reasoning problem to Z3 Python code:

{user_input}

Generate executable Z3 code that determines if the conclusion follows."""
z3_code = llm.generate(prompt)

# 3. 执行代码
result = execute_z3_code(z3_code)  # "True", "False", 或 "Uncertain"

# 4. 如果失败，尝试修复
if result == "ERROR":
    for attempt in range(3):
        fixed_code = llm.repair(z3_code, error)
        result = execute_z3_code(fixed_code)
        if result != "ERROR":
            break
```

### 5. 结果比较

```python
# Green Agent中的比较逻辑
predicted = "True"  # White Agent的回答
expected = "True"   # 标准答案

if predicted == expected:
    correct = True
    print("✓ CORRECT")
else:
    correct = False
    print(f"✗ INCORRECT (expected {expected}, got {predicted})")
```

---

## 关键配置文件

### 1. Agent Card (代理卡片)

每个agent都有一个agent card，定义其能力和协议:

**Baseline Agent Card** (`src/white_agent_baseline/agent.py`):
```python
{
    "name": "baseline_reasoning_agent",
    "description": "Baseline agent that uses direct LLM reasoning",
    "url": "https://folio-baseline-agent-xxxxx.run.app",
    "version": "1.0.0",
    "protocolVersion": "0.3.0",
    "preferredTransport": "JSONRPC",
    "skills": [{
        "id": "logical_reasoning",
        "name": "Logical Reasoning"
    }]
}
```

### 2. 环境变量

**Cloud Run部署时的环境变量**:
```bash
GEMINI_API_KEY=your_key          # Gemini API密钥
PUBLIC_URL=https://...run.app    # 服务的公共URL
CLOUDRUN_HOST=...run.app         # Cloud Run主机名
AGENT_ROLE=baseline              # 或 autoform, green
HOST=0.0.0.0                     # 监听地址
PORT=8080                        # Controller端口
AGENT_PORT=9001                  # Agent内部端口
```

### 3. Dockerfile结构

**关键部分**:
```dockerfile
# 1. 安装依赖
RUN pip install -e .

# 2. 复制代码
COPY . /app

# 3. 设置启动命令 (通过run.sh)
# run.sh会根据AGENT_ROLE启动对应的agent
CMD ["./run.sh"]
```

---

## 常见问题排查

### 1. 本地测试失败

**问题**: Agent无法启动
- 检查: API密钥是否正确设置
- 检查: 端口是否被占用 (9001, 9002, 9003)
- 检查: 依赖是否安装完整

### 2. 云端部署失败

**问题**: 部署脚本报错
- 检查: Google Cloud SDK是否安装
- 检查: 项目ID是否正确
- 检查: API是否已启用 (Cloud Run, Cloud Build)

### 3. AgentBeats平台显示"Controller Reachable: No"

**问题**: 平台无法访问代理
- 解决: 等待30秒 (冷启动延迟)
- 解决: 检查服务URL是否正确
- 解决: 验证服务是否正在运行

### 4. 评估结果不准确

**问题**: 准确率很低
- 检查: LLM API是否正常工作
- 检查: 提示词是否正确
- 检查: 响应解析是否正确

---

## 总结

这个系统实现了一个完整的**AI代理评估框架**:

1. **标准化**: 使用A2A协议和AgentBeats平台
2. **可扩展**: 可以轻松添加新的White Agents
3. **可部署**: 支持本地和云端部署
4. **可比较**: 同时评估多种方法 (Baseline vs Autoformalization)

整个流程从问题输入到结果输出都是自动化的，为逻辑推理任务提供了一个可靠的评估基准。

